{
  "name": "communitymap",
  "private": true,
  "scripts": {
    "postinstall": "npm install --prefix backend && npm install --prefix frontend",
    "dev": "npm run dev:clean && concurrently -k -n backend,frontend -c blue,green \"npm run dev --prefix backend\" \"npm run dev --prefix frontend -- --port 3000\"",
    "dev:clean": "node scripts/clean-dev-ports.cjs",
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend",
    "db:up": "docker compose up -d",
    "db:down": "docker compose down",
    "lint": "npm run lint --prefix frontend",
    "test:smoke": "node scripts/smoke-test.cjs"
  },
  "devDependencies": {
    "concurrently": "^9.2.0",
    "dotenv": "^16.5.0"
  }
}
